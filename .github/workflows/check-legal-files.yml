name: Check legal files
on:
  push:
    branches: [ main, 'feat/*' ]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check legal files
        run: bash scripts/check_legal_files.sh